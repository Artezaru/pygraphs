Installation
============

To install ``pygraphs``, you can use the following command:

.. code-block:: bash

    pip install pygraphs

Or install it directly from the GitHub repository using:

.. code-block:: bash

    pip install git+https://github.com/Artezaru/pygraphs.git

